module.exports = {
  split: function (str) {
    return str.split(',');
  },
  getSplitFirst: function (str) {
    return str.split(',')[0];
  },
  trim: function(str){
    return str.trim();
  },
  //去除所有空格
  // trimAll:function(str){
  //   return str.replace(/\s+/g,"")
  // }
}