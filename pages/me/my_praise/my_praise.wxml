<view class="main">
    <!--我获得的赞-->
    <praise-item contentList="{{contentList}}" offset="{{offset}}"></praise-item>

    <!--懒加载-->
    <view class="ending" wx:if="{{!(bottomFont=='~NOTHING~' && nothingImg !='')}}">
        {{ bottomFont}}
    </view>
    <!--空缺页-->
    <no-thing wx:else src="{{nothingImg}}" text="{{text}}" font="去首页" url="/pages/index/index/index" />
</view>